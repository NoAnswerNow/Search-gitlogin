name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE
      - uses: actions/checkout@v2

      # Set up Python 3.9 environment
      - name: Set up Python 3.9
        uses: actions/setup-python@v1
        with:
          python-version: "3.9"

      # Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Run our unit tests
      - name: Run  tests
        run: |
          pytest -s


      - name: Deploy Herocu
        uses: akhileshns/heroku-deploy@v3.12.12
        with:
            heroku_api_key: ${{secrets.HEROKU_API_KEY}}
            heroku_app_name: "github-login1"
            heroku_email: "kkamera317@gmail.com"
            env_file: ".env"
        env:
            HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}: ${{secrets.API_TOKEN}}


      #- uses: actions/checkout@v1
      #- name: Login to Heroku Container registry
      #  env:
      #    heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
      #  run: heroku container:login
      #- name: Build and push
      #  env:
      #    heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
      #  run: heroku container:push -a github-login1 web
      #- name: Release
      #  env:
      #    HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      #  run: heroku container:release -a github-login1 web
